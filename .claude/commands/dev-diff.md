---
description: Show drift between current project and Dev-harness (read-only)
allowed-tools: Bash
---

managed 인프라 파일 중 하네스와 다른 것들을 나열만 한다 (수정 X).

```bash
bash .claude/bin/dev-sync.sh diff
```

결과 그대로 사용자에게 보고하고 턴 마쳐라.
