# clfx 피드백확장 A단계 (분석·시각화) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 교수님 피드백 A그룹(③접근파일·④actor분리·⑤활동량·⑥키워드·⑦프롬프트요약)을 결정적 엔진 집계 + 신규 `/api/*` + 로컬 LLM 요약으로 구현. 팀원 UI(be9a39b)가 fetch만 교체해 연결.

**Architecture:** 기존 `QueryEngine`에 집계 메서드(activity/files) 추가, 키워드는 신규 `analyze/keywords.py`(결정적), 요약은 `query/llm.py`에 ollama 클라이언트 추가(gemma4:e12b, 미실행 digest fallback). `web/api.py`·`web/server.py`에 신규 엔드포인트. 엔진=단일 진실원천, UI는 표시만.

**Tech Stack:** Python 3 stdlib only(`urllib`로 ollama HTTP), pytest. spec: `docs/superpowers/specs/2026-06-17-clfx-피드백확장-design.md`.

---

## 공통 계약 (Cross-cutting)

- **엔진 무수정 원칙은 "분석/질의 의미 불변"을 뜻한다.** 본 plan은 `analyze`/`query`/`web`에 **신규 집계·엔드포인트를 추가**한다(기존 동작 보존, 회귀 green 유지).
- **actor 분리(④)**: 모든 신규 집계는 `{"user": N, "agent": N}` 형태로 actor를 나눠 반환. 합산값이 아니라 주체별. Event.actor ∈ {user, agent}.
- **secret**: 탐지+마스킹 유지(보안). 태그는 부차 — 신규 집계에서 강조하지 않음.
- **날짜 추출**: Event.ts는 ISO8601 UTC 문자열("2026-06-11T01:00:00.000Z"). day = `ts[:10]`("2026-06-11"), month = `ts[:7]`("2026-06"). ts None → 버킷 `"unknown"`.
- **결정적**: 키워드·활동량·파일 집계는 같은 입력 같은 출력. LLM은 요약 문장에만.
- **C단계(MCP ⑧)·B단계(복구·해시 ①②)·exe 패키징은 별도 plan.** 본 plan 범위 아님.

## File Structure

- Create `clfx/analyze/keywords.py` — 키워드 빈도+수사사전+집중/지속 패턴(결정적, 의존성0).
- Modify `clfx/query/engine.py` — `activity(by)`, `files()` 집계 메서드 추가(actor 분리).
- Modify `clfx/query/llm.py` — `OllamaLLM` 클래스 + `make_llm()` + summarize 연결.
- Modify `clfx/web/api.py` — `activity_payload`, `files_payload`, `keywords_payload`; query_payload가 `make_llm()` 사용.
- Modify `clfx/web/server.py` — `/api/activity`, `/api/files`, `/api/keywords` 라우트.
- Modify `clfx/cli.py` — `cmd_query`가 `make_llm()` 사용(요약에 실제 LLM).
- Test: `tests/test_keywords.py`, `tests/test_engine_aggregates.py`, `tests/test_web_aggregates.py`, `tests/test_llm_ollama.py`.

---

### Task 1: 키워드 분석 (`analyze/keywords.py`)

**Files:**
- Create: `clfx/analyze/keywords.py`
- Test: `tests/test_keywords.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_keywords.py`:

```python
from clfx.event import Event, Source
from clfx.analyze.keywords import keyword_stats


def _ev(actor, target, preview, ts):
    return Event(ts=ts, agent="claude", session="s", actor=actor, action="read",
                 target=target, preview=preview, source=Source("h.jsonl", 1), tags=[])


def test_frequency_and_actor_split():
    evs = [
        _ev("user", "a.py", "비밀번호 유출 확인", "2026-06-11T01:00:00.000Z"),
        _ev("agent", "b.py", "비밀번호 점검", "2026-06-11T02:00:00.000Z"),
        _ev("user", "c.py", "일반 코드 리뷰", "2026-06-11T03:00:00.000Z"),
    ]
    st = keyword_stats(evs)
    kws = {k["term"]: k for k in st["keywords"]}
    # "비밀번호" 3회 등장 안 됨 — 2건(user1/agent1). actor 분리.
    assert kws["비밀번호"]["count"] == 2
    assert kws["비밀번호"]["by_actor"] == {"user": 1, "agent": 1}
    # 수사 위험키워드 플래그
    assert kws["비밀번호"]["investigative"] is True
    assert kws["유출"]["investigative"] is True


def test_concentration_pattern():
    # 같은 키워드가 하루 몰림 → 집중형, 여러 날 분산 → 지속형
    same_day = [_ev("user", "x", "해킹 시도", f"2026-06-11T0{i}:00:00.000Z") for i in range(1, 5)]
    st = keyword_stats(same_day)
    hk = {k["term"]: k for k in st["keywords"]}["해킹"]
    assert hk["pattern"] == "집중형"

    spread = [_ev("user", "x", "해킹 시도", f"2026-06-1{i}T01:00:00.000Z") for i in range(1, 5)]
    st2 = keyword_stats(spread)
    hk2 = {k["term"]: k for k in st2["keywords"]}["해킹"]
    assert hk2["pattern"] == "지속형"


def test_stopword_and_short_filtered():
    evs = [_ev("user", "x", "그 the 이 a 비밀번호", "2026-06-11T01:00:00.000Z")]
    terms = {k["term"] for k in keyword_stats(evs)["keywords"]}
    assert "the" not in terms and "그" not in terms and "a" not in terms
    assert "비밀번호" in terms
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/test_keywords.py -v`
Expected: FAIL — `ModuleNotFoundError: clfx.analyze.keywords`.

- [ ] **Step 3: 구현**

`clfx/analyze/keywords.py`:

```python
"""키워드 빈도 분석(결정적, 의존성0). 증거 집계 — 엔진이 진실원천.
한국어 형태소기 미사용(stdlib only) → 토큰 빈도 + 수사사전 매칭 + 시점 패턴."""
import re
from collections import Counter, defaultdict

# 수사 관점 위험 키워드(한국어+영어). 매칭되면 investigative=True 강조.
INVESTIGATIVE = {
    "비밀번호", "패스워드", "password", "주민", "주민등록", "계좌", "카드번호",
    "유출", "해킹", "공격", "탈취", "권한", "루트", "root", "sudo", "토큰", "token",
    "api", "key", "credential", "계정", "로그인", "개인정보", "기밀", "secret", "유출됨",
}
# 불용어(조사·관사·일반어). 한국어 정밀도 한계는 spec §6에 명시됨.
_STOP = {
    "그", "이", "저", "것", "수", "등", "및", "the", "a", "an", "of", "to", "in",
    "is", "and", "or", "에", "는", "은", "이", "가", "을", "를", "도", "로", "으로",
}
_TOKEN = re.compile(r"[A-Za-z0-9_]+|[가-힣]+")


def _tokens(text):
    out = []
    for t in _TOKEN.findall(text or ""):
        tl = t.lower()
        if len(t) < 2 or tl in _STOP:
            continue
        out.append(t)
    return out


def keyword_stats(events, top=50):
    """이벤트(preview+target)에서 키워드 빈도·actor분리·수사플래그·시점패턴 집계.
    반환: {"keywords": [{term,count,by_actor,investigative,pattern,days}, ...]}"""
    count = Counter()
    by_actor = defaultdict(lambda: {"user": 0, "agent": 0})
    by_day = defaultdict(Counter)            # term -> {day: n}
    for e in events:
        text = f"{e.target or ''} {e.preview or ''}"
        day = (e.ts or "")[:10] or "unknown"
        seen = set(_tokens(text))            # 이벤트당 1회(중복 토큰 한 번)
        for term in seen:
            count[term] += 1
            actor = e.actor if e.actor in ("user", "agent") else "user"
            by_actor[term][actor] += 1
            by_day[term][day] += 1
    kws = []
    for term, c in count.most_common(top):
        days = by_day[term]
        # 집중형: 한 날이 전체의 절반 이상. 지속형: 분산. (단일 등장은 집중형.)
        pattern = "집중형" if max(days.values()) / sum(days.values()) >= 0.5 else "지속형"
        kws.append({
            "term": term, "count": c, "by_actor": dict(by_actor[term]),
            "investigative": term.lower() in {w.lower() for w in INVESTIGATIVE},
            "pattern": pattern, "days": len(days),
        })
    return {"keywords": kws}
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/test_keywords.py -v`
Expected: PASS (3 test).

- [ ] **Step 5: 커밋**

```bash
git add clfx/analyze/keywords.py tests/test_keywords.py
git commit -m "feat(analyze): 키워드 빈도+수사사전+시점패턴 (결정적, 피드백⑥)"
```

---

### Task 2: 엔진 활동량 집계 `activity(by)` (⑤④)

**Files:**
- Modify: `clfx/query/engine.py`
- Test: `tests/test_engine_aggregates.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_engine_aggregates.py`:

```python
from clfx.event import Event, Source
from clfx.query.engine import QueryEngine


def _ev(actor, action, ts, target="x"):
    return Event(ts=ts, agent="claude", session="s", actor=actor, action=action,
                 target=target, preview="", source=Source("h.jsonl", 1), tags=[])


def _eng():
    return QueryEngine([
        _ev("user", "paste", "2026-06-11T01:00:00.000Z"),
        _ev("agent", "read", "2026-06-11T02:00:00.000Z"),
        _ev("agent", "read", "2026-07-02T02:00:00.000Z"),
        _ev("user", "prompt", None),  # ts 없음 → unknown
    ])


def test_activity_by_day_actor_split():
    a = _eng().activity(by="day")
    # day 버킷별 actor 분리
    d = {row["bucket"]: row for row in a}
    assert d["2026-06-11"]["user"] == 1 and d["2026-06-11"]["agent"] == 1
    assert d["2026-07-02"]["agent"] == 1 and d["2026-07-02"]["user"] == 0
    assert d["unknown"]["user"] == 1


def test_activity_by_month():
    a = _eng().activity(by="month")
    d = {row["bucket"]: row for row in a}
    assert d["2026-06"]["user"] == 1 and d["2026-06"]["agent"] == 1
    assert d["2026-07"]["agent"] == 1
    # 버킷은 정렬됨(unknown 맨 뒤)
    buckets = [row["bucket"] for row in a]
    assert buckets == sorted([b for b in buckets if b != "unknown"]) + (["unknown"] if "unknown" in buckets else [])
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/test_engine_aggregates.py::test_activity_by_day_actor_split -v`
Expected: FAIL — `AttributeError: 'QueryEngine' object has no attribute 'activity'`.

- [ ] **Step 3: 구현**

`clfx/query/engine.py` 맨 위 import에 추가:

```python
from collections import defaultdict
```

`QueryEngine` 클래스에 메서드 추가(`timeline` 아래):

```python
    def activity(self, by="day"):
        """활동량 집계(actor 분리). by=day → ts[:10], month → ts[:7].
        반환: [{"bucket": "2026-06-11", "user": N, "agent": N, "total": N}, ...] (bucket 정렬, unknown 맨뒤)."""
        n = 10 if by == "day" else 7
        buckets = defaultdict(lambda: {"user": 0, "agent": 0})
        for e in self.events:
            b = (e.ts or "")[:n] or "unknown"
            actor = e.actor if e.actor in ("user", "agent") else "user"
            buckets[b][actor] += 1
        known = sorted(b for b in buckets if b != "unknown")
        order = known + (["unknown"] if "unknown" in buckets else [])
        return [{"bucket": b, "user": buckets[b]["user"], "agent": buckets[b]["agent"],
                 "total": buckets[b]["user"] + buckets[b]["agent"]} for b in order]
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/test_engine_aggregates.py -v`
Expected: PASS (activity 2 test).

- [ ] **Step 5: 커밋**

```bash
git add clfx/query/engine.py tests/test_engine_aggregates.py
git commit -m "feat(engine): activity(by) 활동량 집계 actor 분리 (피드백⑤④)"
```

---

### Task 3: 엔진 접근파일 목록 `files()` (③④)

**Files:**
- Modify: `clfx/query/engine.py`
- Test: `tests/test_engine_aggregates.py`

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_engine_aggregates.py` 에 추가:

```python
def test_files_grouping_actor_split():
    eng = QueryEngine([
        _ev("user", "paste", "2026-06-11T01:00:00.000Z", target=".env"),
        _ev("agent", "read", "2026-06-11T02:00:00.000Z", target=".env"),
        _ev("agent", "read", "2026-06-11T03:00:00.000Z", target="app.py"),
    ])
    fs = {row["target"]: row for row in eng.files()}
    assert fs[".env"]["count"] == 2
    assert fs[".env"]["by_actor"] == {"user": 1, "agent": 1}
    assert fs[".env"]["actions"] == {"paste": 1, "read": 1}
    assert fs["app.py"]["by_actor"] == {"user": 0, "agent": 1}
    # 접근 많은 순 정렬
    assert eng.files()[0]["target"] == ".env"
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/test_engine_aggregates.py::test_files_grouping_actor_split -v`
Expected: FAIL — `AttributeError: ... 'files'`.

- [ ] **Step 3: 구현**

`clfx/query/engine.py` `QueryEngine`에 메서드 추가:

```python
    _FILE_ACTIONS = ("read", "write", "paste", "upload")

    def files(self):
        """접근 파일 목록(actor 분리·action별·태그). read/write/paste/upload만.
        반환: [{"target", "count", "by_actor", "actions": {action:n}, "tags": [..]}, ...] (접근 많은 순)."""
        agg = {}
        for e in self.events:
            if e.action not in self._FILE_ACTIONS or not e.target:
                continue
            r = agg.setdefault(e.target, {"target": e.target, "count": 0,
                                          "by_actor": {"user": 0, "agent": 0},
                                          "actions": {}, "tags": set()})
            r["count"] += 1
            actor = e.actor if e.actor in ("user", "agent") else "user"
            r["by_actor"][actor] += 1
            r["actions"][e.action] = r["actions"].get(e.action, 0) + 1
            for t in (e.tags or []):
                r["tags"].add(t)
        rows = sorted(agg.values(), key=lambda r: (-r["count"], r["target"]))
        for r in rows:
            r["tags"] = sorted(r["tags"])
        return rows
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/test_engine_aggregates.py -v`
Expected: PASS (activity + files 전부).

- [ ] **Step 5: 커밋**

```bash
git add clfx/query/engine.py tests/test_engine_aggregates.py
git commit -m "feat(engine): files() 접근파일 목록 actor 분리 (피드백③④)"
```

---

### Task 4: 로컬 LLM 요약 (`OllamaLLM` + `make_llm`) (⑦)

**Files:**
- Modify: `clfx/query/llm.py`
- Test: `tests/test_llm_ollama.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_llm_ollama.py`:

```python
from clfx.event import Event, Source
from clfx.query.llm import OllamaLLM, make_llm, summarize


def _ev():
    return Event(ts="2026-06-11T01:00:00.000Z", agent="claude", session="s",
                 actor="agent", action="read", target=".env", preview="x",
                 source=Source("h.jsonl", 7), tags=[])


class _FakeLLM:
    def __init__(self): self.called = False
    def complete(self, prompt):
        self.called = True
        return "요약문 (h.jsonl:7)"


def test_summarize_uses_llm_when_present():
    llm = _FakeLLM()
    out = summarize([_ev()], llm=llm)
    assert llm.called and out["mode"] == "llm"
    assert out["citations"] == ["h.jsonl:7"]


def test_summarize_falls_back_when_llm_raises():
    class Dead:
        def complete(self, p): raise RuntimeError("ollama down")
    out = summarize([_ev()], llm=Dead())
    assert out["mode"] == "digest"          # 죽으면 digest


def test_make_llm_default_returns_ollama_client():
    llm = make_llm()
    assert isinstance(llm, OllamaLLM) and llm.model == "gemma4:e12b"


def test_make_llm_disabled_returns_none():
    assert make_llm(use_ollama=False) is None


def test_ollama_complete_builds_request(monkeypatch):
    # urlopen을 가짜로 — 실제 ollama 없이 요청 구성·파싱 검증
    import clfx.query.llm as m
    captured = {}
    class _Resp:
        def __enter__(self): return self
        def __exit__(self, *a): return False
        def read(self): return b'{"response": "hi"}'
    def fake_urlopen(req, timeout=None):
        captured["url"] = req.full_url
        captured["body"] = req.data
        return _Resp()
    monkeypatch.setattr(m.urllib.request, "urlopen", fake_urlopen)
    out = OllamaLLM().complete("prompt-x")
    assert out == "hi"
    assert captured["url"].endswith("/api/generate")
    assert b"gemma4:e12b" in captured["body"] and b"prompt-x" in captured["body"]
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/test_llm_ollama.py -v`
Expected: FAIL — `ImportError: cannot import name 'OllamaLLM'`.

- [ ] **Step 3: 구현**

`clfx/query/llm.py` 상단 import에 추가:

```python
import json
import urllib.request
```

파일 끝에 추가:

```python
class OllamaLLM:
    """로컬 ollama 요약 클라이언트. 증거 외부전송 0(localhost).
    complete()가 실패하면 summarize가 digest로 폴백한다."""

    def __init__(self, model="gemma4:e12b", host="http://localhost:11434", timeout=60):
        self.model = model
        self.host = host.rstrip("/")
        self.timeout = timeout

    def complete(self, prompt):
        body = json.dumps({"model": self.model, "prompt": prompt, "stream": False}).encode("utf-8")
        req = urllib.request.Request(self.host + "/api/generate", data=body,
                                     headers={"Content-Type": "application/json"})
        with urllib.request.urlopen(req, timeout=self.timeout) as r:
            return json.loads(r.read().decode("utf-8")).get("response", "")


def make_llm(use_ollama=True):
    """요약용 LLM 어댑터. ollama 미사용/미실행이면 None(→ summarize가 digest).
    OllamaLLM은 호출 시점에 연결하므로 여기서 살아있는지 검사하지 않는다(complete 실패→fallback)."""
    if not use_ollama:
        return None
    return OllamaLLM()
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/test_llm_ollama.py -v`
Expected: PASS (5 test).

- [ ] **Step 5: 커밋**

```bash
git add clfx/query/llm.py tests/test_llm_ollama.py
git commit -m "feat(llm): OllamaLLM(gemma4:e12b) 로컬 요약 + make_llm, digest fallback (피드백⑦)"
```

---

### Task 5: 웹 API payload (activity/files/keywords) + 요약 LLM 연결

**Files:**
- Modify: `clfx/web/api.py`
- Test: `tests/test_web_aggregates.py`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_web_aggregates.py`:

```python
from clfx.event import Event, Source
from clfx.query.engine import QueryEngine
from clfx.web.api import activity_payload, files_payload, keywords_payload


def _ev(actor, action, ts, target="x", preview=""):
    return Event(ts=ts, agent="claude", session="s", actor=actor, action=action,
                 target=target, preview=preview, source=Source("h.jsonl", 1), tags=[])


def _eng():
    return QueryEngine([
        _ev("user", "paste", "2026-06-11T01:00:00.000Z", ".env", "비밀번호 유출"),
        _ev("agent", "read", "2026-06-11T02:00:00.000Z", ".env", "점검"),
    ])


def test_activity_payload():
    p = activity_payload(_eng(), by="month")
    assert p["by"] == "month"
    assert p["rows"][0]["bucket"] == "2026-06"
    assert p["rows"][0]["user"] == 1 and p["rows"][0]["agent"] == 1


def test_files_payload():
    p = files_payload(_eng())
    assert p["files"][0]["target"] == ".env" and p["files"][0]["count"] == 2


def test_keywords_payload():
    p = keywords_payload(_eng())
    terms = {k["term"] for k in p["keywords"]}
    assert "비밀번호" in terms
    assert any(k["investigative"] for k in p["keywords"])
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/test_web_aggregates.py -v`
Expected: FAIL — `ImportError: cannot import name 'activity_payload'`.

- [ ] **Step 3: 구현**

`clfx/web/api.py` 상단 import 교체/추가:

```python
from clfx.query.llm import route_intent, summarize, make_llm
from clfx.analyze.keywords import keyword_stats
```

함수 추가:

```python
def activity_payload(engine, by="day"):
    """활동량 집계 — UI 히트맵용. actor 분리(④)."""
    by = by if by in ("day", "month") else "day"
    return {"by": by, "rows": engine.activity(by=by)}


def files_payload(engine):
    """접근파일 목록 — UI 파일패널용. actor 분리(③④)."""
    return {"files": engine.files()}


def keywords_payload(engine):
    """키워드 빈도 집계 — UI 도넛용(⑥). 결정적, 수사사전·패턴 포함."""
    return keyword_stats(engine.events)
```

`query_payload` 의 summarize 호출을 LLM 연결로 교체(요약 요청 시 실제 ollama 사용):

```python
    summary = summarize(res, llm=make_llm()) if intent.get("summarize") else None
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/test_web_aggregates.py tests/test_web_api.py -v`
Expected: PASS (신규 3 + 기존 web_api 회귀). 기존 query_payload 테스트는 ollama 미실행이라 summarize가 digest fallback → mode 검증 영향 없으면 green. (기존 테스트가 mode=="digest"를 단언했다면 ollama 없는 CI에서 그대로 통과.)

- [ ] **Step 5: 커밋**

```bash
git add clfx/web/api.py tests/test_web_aggregates.py
git commit -m "feat(web): activity/files/keywords payload + 요약 LLM 연결 (피드백③⑤⑥⑦)"
```

---

### Task 6: 웹 서버 라우트 (/api/activity, /api/files, /api/keywords)

**Files:**
- Modify: `clfx/web/server.py`
- Test: `tests/test_web_server.py`

- [ ] **Step 1: 실패 테스트 추가**

`tests/test_web_server.py` 에 추가(기존 `_server()`/`_get()` 헬퍼 재사용):

```python
def test_activity_endpoint():
    httpd = _server()
    try:
        code, body = _get(httpd, "/api/activity?by=month")
        assert code == 200
        import json as _j
        d = _j.loads(body)
        assert d["by"] == "month" and isinstance(d["rows"], list)
    finally:
        httpd.shutdown()


def test_files_endpoint():
    httpd = _server()
    try:
        code, body = _get(httpd, "/api/files")
        assert code == 200
        import json as _j
        assert "files" in _j.loads(body)
    finally:
        httpd.shutdown()


def test_keywords_endpoint():
    httpd = _server()
    try:
        code, body = _get(httpd, "/api/keywords")
        assert code == 200
        import json as _j
        assert "keywords" in _j.loads(body)
    finally:
        httpd.shutdown()
```

- [ ] **Step 2: 실패 확인**

Run: `pytest tests/test_web_server.py -v -k "activity or files or keywords"`
Expected: FAIL — 404(라우트 없음)라 `assert code == 200` 실패.

- [ ] **Step 3: 구현**

`clfx/web/server.py` 상단 import 교체:

```python
from clfx.web.api import (events_payload, query_payload,
                          activity_payload, files_payload, keywords_payload)
```

`do_GET` 의 `/api/query` 블록 아래, `self._json({"error": "not found"}, 404)` **앞에** 추가:

```python
            if u.path == "/api/activity":
                by = (parse_qs(u.query).get("by") or ["day"])[0]
                try:
                    self._json(activity_payload(engine, by=by))
                except Exception as e:
                    self._json({"error": str(e)}, 500)
                return
            if u.path == "/api/files":
                try:
                    self._json(files_payload(engine))
                except Exception as e:
                    self._json({"error": str(e)}, 500)
                return
            if u.path == "/api/keywords":
                try:
                    self._json(keywords_payload(engine))
                except Exception as e:
                    self._json({"error": str(e)}, 500)
                return
```

- [ ] **Step 4: 통과 확인**

Run: `pytest tests/test_web_server.py -v`
Expected: PASS (신규 3 + 기존 통합 전부).

- [ ] **Step 5: 커밋**

```bash
git add clfx/web/server.py tests/test_web_server.py
git commit -m "feat(web): /api/activity·files·keywords 라우트 (피드백③⑤⑥)"
```

---

### Task 7: CLI query 요약 LLM 연결

**Files:**
- Modify: `clfx/cli.py`

- [ ] **Step 1: 회귀 기준선 확인**

Run: `pytest tests/test_cli_query.py -q`
Expected: PASS(현행). 이 Task는 요약 시 실제 LLM을 쓰되 ollama 없으면 digest라 출력 형식 불변.

- [ ] **Step 2: cmd_query가 make_llm 사용하도록**

`clfx/cli.py` 의 `cmd_query`는 `query_payload`를 호출하므로(Stage4에서 위임됨) **이미 Task5에서 make_llm이 연결됨** — 추가 코드 변경 없음. 단 import 정합 확인: `cli.py`가 `query_payload`만 호출하면 OK. 직접 summarize 호출이 남아있으면 제거.

`clfx/cli.py`에서 `cmd_query` 본문이 `query_payload(eng, args.question)`만 쓰는지 확인(Stage4 리팩토링 상태). 그렇다면 코드 변경 없이 Task5의 make_llm 연결이 CLI에도 적용됨.

- [ ] **Step 3: 회귀 확인**

Run: `pytest tests/test_cli_query.py tests/test_e2e_ab.py -q`
Expected: PASS. ollama 없는 환경 → digest fallback이라 기존 출력·인용 검증 그대로 통과.

- [ ] **Step 4: 전체 회귀**

Run: `pytest -q`
Expected: PASS(전부). 기존 + 신규.

- [ ] **Step 5: 커밋(변경 있을 때만)**

```bash
git add clfx/cli.py 2>/dev/null
git commit -m "chore(cli): query 요약 LLM 연결 정합 확인 (피드백⑦)" --allow-empty
```

---

## Acceptance (A단계 전체)

```bash
pytest tests/test_keywords.py tests/test_engine_aggregates.py tests/test_web_aggregates.py tests/test_llm_ollama.py tests/test_web_server.py -q
```

전체 회귀도 green:

```bash
pytest -q
```

## Self-Review 결과

- **Spec coverage**: ③파일목록(Task3 `files()`+Task5/6 payload·route)·④actor분리(Task2/3 by_actor)·⑤활동량(Task2 `activity`+route)·⑥키워드(Task1 keywords+Task5/6)·⑦요약(Task4 OllamaLLM+Task5 연결). 전부 매핑. secret=탐지/마스킹 유지·강조X(신규 집계는 actor·키워드 중심).
- **Placeholder**: 없음(모든 코드 전체 기재).
- **Type 일관성**: `activity(by)`→`{bucket,user,agent,total}`, `files()`→`{target,count,by_actor,actions,tags}`, `keyword_stats`→`{keywords:[{term,count,by_actor,investigative,pattern,days}]}`, `make_llm()`→OllamaLLM|None, `summarize(events,llm)`(기존). payload들이 이 형태를 그대로 감쌈. 일관.
- **신규 API ↔ 팀원 UI**: `/api/activity?by=`, `/api/keywords`, `/api/files` — UI-변경사항.md §4 요청과 경로·의미 일치. UI는 fetch만 교체.
- **범위**: C(MCP)·B(복구·해시)·exe는 별도 plan 명시.
